import { FormatonombrePipe } from './formatonombre.pipe';

describe('FormatonombrePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatonombrePipe();
    expect(pipe).toBeTruthy();
  });
});
